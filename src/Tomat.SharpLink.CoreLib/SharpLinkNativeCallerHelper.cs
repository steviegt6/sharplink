namespace Tomat.SharpLink;

public static class SharpLinkNativeCallerHelper {
    public static object? CallNative(string lib, string name, params object[] args) {
        // TODO: implement this when we move onto the runtime
        return null;
    }
}
